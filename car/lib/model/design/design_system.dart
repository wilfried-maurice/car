import 'package:flutter/material.dart';

/// DesignSystem related to the IFEA-APP brand.
class DesignSystem {
  /// button appearance Used for background scaffold or dark

  static const buttonColor = Color(0xFF1b61fb);
  static const buttonColor2 = Color(0xFF1a1d27);

  static const bgndapp = Color(0xFF002de0);
  static const backgrnd1 = Color(0xFF0d102c);
  static const backgrnd2 = Color(0xFF0e0c23);
  static const backgrnd3 = Color(0xFF0e0f1a);

  /// Shade1 for primary color. Used for light text appearance and icons.
  static const iconColor = Color(0xFF6b9cfb);

  /// Shade2 for primary color.
  static const titleColor = Color(0xFFe7e7e7);
  static const titleColor2 = Color(0xFF88b8b9);

  /// Shade2 for primary color.
  static const container = Color(0xFF0e0c23);
  static const container1 = Color(0xFFb0adc9);
  static const container2 = Color(0xFF556089);

  /// Error color
  static const errorColor = Color(0xFFE03B3B);

  /// Warning color
  static const warningColor = Color(0xFFAB9A00);

  /// Success color
  static const snackbarColor = Color(0xFF388F40);

  /// Size big title
  static const xsmall = 25.0;

  /// Size subtitle
  static const small = 12.0;

  /// Size paragraph
  static const medium = 13.0;

  /// Size title
  static const large = 14.0;
}
